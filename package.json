{
  "name": "minnebo-ai",
  "version": "0.1.0",
  "homepage": "https://minnebo.ai",
  "dependencies": {
    "@google/generative-ai": "^0.1.3",
    "@types/node": "^18.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "framer-motion": "^12.23.22",
    "gh-pages": "^5.0.0",
    "html2canvas": "^1.4.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "react-share": "^5.0.3",
    "sharp": "^0.33.4",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@vercel/node": "^2.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "mkdir -p build && echo '<html><body>API only</body></html>' > build/index.html",
    "build:react": "react-scripts build",
    "predeploy": "npm run build:react",
    "deploy": "gh-pages -d build",
    "deploy:ci": "npm run build:react && gh-pages -d build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
